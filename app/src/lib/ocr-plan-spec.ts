/**
 * 確定仕様の件数。**掲載文と課金の約束そのもの。**
 *
 * 🔴 ここを唯一の出どころにする。`ai-guard.ts` の既定値もこれを使う。
 * 既定が仕様と違うと、env の設定漏れが**静かに違う商品**になる。
 *
 * 🔴 **無料は 2026-09-21 に 5 → 15 へ（利用者の決定）。恒久の価格ではない。**
 *
 * 理由は**測れないから**。実利用者 3 人のうち 2 人が 5 件ちょうどで止まり、
 * 1 人は上限に当たった直後（`toritavi_ai_rejections` の `quota_units` が 2 件）
 * から再ログインしていない。**全員が 5 で切られるので「本当は何件要るのか」が
 * 観測できず、次の価格を決める根拠が作れない。**
 * 原価は制約になっていない —— 1 件の実測は 3.77 セント（約 ¥5.7）で、
 * 全利用者の月額合計は $1.59〜$2.91。
 *
 * 🔴 **見直しは「実利用者 20〜30 人」か「2026-11-30」の早い方。**
 * 期限を過ぎたら、測った結果で決め直すこと。放っておくと
 * 「暫定のまま恒久になった数字」になる（`monetization-spec.md` §288 が、
 * **5 件には導出の記録が無い**と書いているのと同じ形）。
 *
 * 🔴 **ここを動かすと公開文書 5 か所が連動する。** 特商法・LP 日英・台帳・
 * App Store 掲載文。`toritavi_app` の
 * `test/docs/tokushoho_matches_implementation_test.dart` が
 * **このファイルから数字を読んで**特商法ページと突き合わせるので、
 * 片方だけ変えると落ちる。**それが狙いの仕掛け。**
 */
export const SPEC_FREE_REQUESTS = 15;
export const SPEC_PRO_REQUESTS = 50;
export const SPEC_GUEST_REQUESTS = 3;
