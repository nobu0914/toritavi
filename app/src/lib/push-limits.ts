/**
 * プッシュ通知の文字数上限。**1 か所に置く。**
 *
 * 個別通知（`/api/admin/users/[id]/notify`）は 80 / 300 で弾いていたが、
 * 全員宛の一斉送信（`/api/push/broadcast`）には**検証が 1 つも無かった**
 * （2026-08-16 の検査 L-6）。影響が大きいほうが緩い、という逆転。
 *
 * 端末側は長い通知を途中で切って表示するので、長すぎるものは
 * 「送れたが読めない」になる。FCM 全体の 4KB 制限にも近づく。
 *
 * 🔴 **画面の `maxLength` は上限ではない。** あれは打ち間違いを防ぐだけで、
 * API を直接叩けば素通りする。**必ずサーバ側で弾く。**
 */
export const PUSH_TITLE_MAX = 80;
export const PUSH_BODY_MAX = 300;
