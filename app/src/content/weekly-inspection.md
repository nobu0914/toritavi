# 週次検査（Fable 5 に検査させ、Opus が直す）

最終更新: 2026-09-13

> **この文書が正本。** サーバ側 `src/content/weekly-inspection.md` はコピーで、
> 管理画面 `/admin/maintenance` のタブとして表示するためだけに置いてある。
> 直すときは必ずこちら。

## 週次検査（Fable）

**Fable は検査だけ。実行と修正は Opus。** 毎週 1 回、下の指示ブロックを 1 つ
貼る。レーン 1〜8 を Fable が順に回す。

### なぜこの軸か

**「バグ」より「静かな不一致」で痛い目に遭ってきた。** 一般的なコードレビューの
軸をそのまま渡すと、既に 800 件超のテストが守っている範囲を再走査して終わる。
だから軸は**テストが構造的に見られない所**へ寄せてある。

**過去の外部監査は 9 件中そのまま正しかったのが 3 件だった。** 出力は「所見」で
あって「事実」ではない前提で、**検証しやすい形を強制する**のがこの手順の肝。
とくに所見 1 件ごとの「これが偽である条件」を必ず書かせること。これが無いと
Opus 側の検証コストが跳ね上がる。

### 回し方（Fable をどこで動かすか）

**A. Opus に「週次検査を回して」と言う（普段はこちら）**

Opus が下の指示文を Fable 5 のサブエージェントへ渡し、返ってきた所見を
そのまま受け取って真偽判定 → 修正まで一続きでやる。**コピペが要らない。**
レーンごとに別のサブエージェントへ振れるので、1 セッションに全部を詰め込む
必要もない。

独立性は保たれる。サブエージェントは**まっさらな文脈で指示文だけを受け取る**
ので、Opus の思い込みを引き継がない。

**B. 別セッションで自分で回す（出先・別マシン）**

1. **`~/Dev/toritavi_app` を作業ディレクトリにして新しいセッションを開く**
2. モデルを **Fable 5** に切り替える（アプリのモデル選択から）
3. 管理画面 `/admin/maintenance` →「週次検査」タブ → 下のブロックをコピーして貼る
4. 出た所見を Opus のセッションへ貼る

> 🔴 **claude.ai のチャットに貼っても動かない。** コードを読む必要があるので、
> **リポジトリを作業ディレクトリにした Claude Code のセッション**であること。
> ここを外すと、読めないまま「それらしい所見」が返ってくる——監査としては
> 最悪の壊れ方をする。

### 今週（2026-09-13）の重点 —— レーン 7 と 8 だけ

**利用者の指示**: ①利用者から見てスピードが遅くなる所 ②サービスとして
仕様上の辻褄が合っていない所。この 2 つのために**レーン 7・8 を新設した。**

**回し方**: 下のブロックをそのまま貼って、「**レーン 7 と 8 だけ**」と
言い足す（§「1 レーンだけ回すとき」と同じやり方）。

**なぜ 1〜6 を飛ばすか**: 1 は 2 周連続ゼロで枯れている。2〜5 は
直近 2 週で回しており、今週の狙いと重ならない。**新しい軸を 2 本、
深く回す方が取れる。**

🔴 **7 と 8 は初回。** 初回のレーンは所見が多く出て、**偽も混ざる**
（レーン 4 の初回は 9 件中 9 件が真だったが、外部監査は 9 件中 3 件だった）。
**真偽判定を飛ばさない。** 偽が半分を超えたら、直すのではなく
**そのレーンの指示文を書き直す。**

### 検査指示（これ 1 つを貼る）

```
あなたは JUNROS（Flutter + Supabase の iOS 旅程アプリ）のコードを検査する。
検査だけを行う。

━━━ 進め方 ━━━
レーン 1 → 8 を順に 1 つずつ。レーンを終えるたびに、そのレーンの所見を
出してから次へ進む。全レーンを読んでから始めない（注意が散る）。

文脈が足りなくなりそうになったら、そこで止めて「レーン N まで完了」と
書く。黙って浅くしないこと。残りは新しいセッションで「レーン N+1 から」と
指示すれば続けられる。

特定のレーンだけを指示された場合は、それだけやる。

各レーンは所見が出なくなるまで探す。1 周で終わりにせず、2 周連続で新しい
所見がゼロになったら「枯れた」と書いて次のレーンへ進む。件数の目標は置かない
（目標を置くと尻尾を取りこぼす）。

最後に「見ていない範囲」を明記する。

━━━ 先に読むもの ━━━
- CLAUDE.md（とくに §5 判断の原則 / §6 既知の危険物 / §6-1 沈黙の検査項目）
- docs/ の正本（service-spec / monetization-spec / feature-flags /
  travel-info-safety / data-privacy-spec）
- test/ のファイル名一覧（何が既に守られているかを把握するため）

━━━ 禁止事項 ━━━
1. コードを変更しない。パッチ・差分・修正案を書かない。
   直し方を決めるのは実装側の判断であり、監査が直し方まで書くと
   「もっともらしい修正」に引っ張られる。所見は
   「どこが・どういう入力で・どう壊れるか」まで。
2. テストが緑であることを、正しさの根拠にしない。
   このリポジトリでは実際に、テストが矛盾の誤った側を正解として固定していた。
3. 本番 DB に接続しない。SQL を実行しない。
4. リポジトリに定義が無い本番オブジェクト（increment_ocr_usage 等の DB 関数）
   について、コードだけを見て断定しない。該当したら
   「DB 側の確認が必要」と書く。
5. 推測を事実として書かない。推測なら「推測」と明記する。
6. 既存テストが既に守っている事柄を所見にしない。
   守れていないと考えるなら、なぜ通ってしまうかを書く。

━━━ 所見の書式（1 件ごとに必ずこの 7 項目）━━━
- 分類 / 深刻度
- 場所: file:line
- 主張: 1 文。「〜が〜のとき〜になる」
- 壊れ方: 具体的な入力 → 実際に起きること（画面・DB・ログのどれか）
- 根拠: コードの引用。推測なら「推測」と明記
- これが偽である条件: 私が最初に確認すべき 1 点
- 既存テストの状態: 守られている / 通ってしまう / 該当なし

深刻度:
  致命 = 写真・PII の流出、課金の嘘、上限が効かない、データ消失
  高   = 機能が黙って消えている、正本と実装の食い違いで利用者が騙される
  中   = 文書間の矛盾、いま動くが次の変更で壊れる形
  低   = 表記ゆれ、読みにくさ

━━━━━━━━━━ レーン 1: 沈黙（出ないのに落ちない）━━━━━━━━━━

機能が消えても例外が出ない形。呼び出し側は実行したつもりで、画面には何も
起きず、テストも通る。このリポジトリで最も見つけにくく、最も痛い。

実際に起きたもの（校正用。同じ形が他にないか探す）:
- 削除の Undo トーストが 8 箇所すべて出ていなかった
  → Overlay.maybeOf が null を返し、黙って return していた
- 7 か国の入国要件が丸ごと無効
  → applies_to が空＝フェイルクローズで対象外扱いになっていた
- OCR 上限が毎日 9 時間効いていなかった
  → 複製した SQL 関数に、既知の JST 修正が入っていなかった

見る観点:
1. 早期 return・空 catch・?? のフォールバック・orElse・maybeOf・
   フェイルクローズが「無言の出口」になっていないか。
   出ないことが正しいなら、その理由がコメントに書いてあるか。
2. 同じ経路を通る呼び出しの数が揃っているか。
   1 箇所で沈黙していたら、だいたい全部沈黙している
   （トーストは 8 箇所、入国要件は 5 か国だった）。
3. 定数表・対応表の網羅。片方に足してもう片方に足し忘れる形
   （entry_requirements に 7 か国を足したとき kCountrySupport に
   足し忘れた、が実例）。
4. 同じ処理が 2 か所以上にある箇所。既知の修正が全部に入っているか。
   とくに時刻・タイムゾーン・上限。
5. 「出る」ことをテストが固定しているか。
   「落ちない」テストは、出ないことも通す。
6. **状態が自動で進むか。** 状態の集合（`kStatuses` のような列挙）に対して、
   **そこへ入る経路が実装されているか**を 1 つずつ数える。
   実例（2026-08-16・利用者から報告）: 予定の状態に `進行中` があり、
   タイムラインの丸も `進行中` を描き分ける実装が入っていたのに、
   **自動で入る経路が無かった**。自動で入るのは `完了` だけで、
   開始時刻を過ぎても丸は空のまま。**落ちも警告も出ない。**
   `kConciergeEnabled` が「定義はあるのに誰も読んでいない」だった件と同型で、
   こちらは「状態はあるのに誰も書かない」。
   🔴 **足す側を直したら、受ける側も数える。** 上の件では
   「`未開始` の予定だけ完了にする」実装が残っており、そのままだと
   **一度 `進行中` になった予定は二度と完了しない**。対で直す。

出し方: 無言の出口は数が多い。全部を所見にせず、
「出ないと利用者が損をする」ものだけを所見にする。それ以外は末尾に
「正当な沈黙と判断したもの」として場所だけ列挙する。

━━━━━━━━━━ レーン 2: 整合性 ━━━━━━━━━━

文書とコード、文書と文書、内部値と表示名の食い違い。

見る観点:
1. 正本文書 ↔ コード。
   数字（上限・価格・保持日数）、フラグ名と既定値、カテゴリ名、状態名。
   数字はコードが正しいとは限らない。どちらが正本かを文書側の宣言
   （「ここが正本」）で判定する。
2. 文書 ↔ 文書。
   同じ事項を 2 か所以上が定義していないか。定義しているなら食い違って
   いないか。2026-08-02 にフェーズ定義が 5 文書で割れていたのを直した
   ばかりで、同じ構造が他にも残っている可能性がある。
3. 内部値 ↔ 表示名。
   - 内部コード名 toritavi が利用者の目に触れていないか（対ユーザー名は JUNROS）
   - Step.category の生値（飛行機・宿泊）が画面に出ていないか
     （表示は categoryLabel() を通した フライト・ホテル）
   - 旅程の状態の内部値「完了」が画面に出ていないか（表示は「終了」）
   同種の変換が他にもあれば、その出口が 1 か所に集約されているかを見る。
4. 公開文書 ↔ 実装。
   未稼働の機能を公開文書（App Store 掲載文・プロモ文言・プライバシー）に
   書いていないか。逆に、実装済みで開く予定のものを「言ってはいけない」
   一覧に残していないか。

━━━━━━━━━━ レーン 3: 課金と上限 ━━━━━━━━━━

🔴 **課金はもう動いている**（2026-09-04 更新）。`kSubscriptionEnabled` は
**2026-09-02 に true**。1.1.0 が **09-04 17:01 JST に公開**され、同日 18:05 に
**実購入が本番で成立**した（購入 → RevenueCat → webhook → `toritavi_user_plan`
まで実測）。**解約も通した**（`CANCELLATION` は行に触らない ＝ pro のまま）。

つまりここは**「開ける前に安く直す」段階を過ぎた。** いま壊れると、
**払った人に影響する。** 見る目線を「開けたら何が壊れるか」から
**「払った人が損をする経路はどれか」**へ移すこと。

> ここは 2026-08-30 まで「2026-08-16 に Phase 2 へ差し戻した」、
> 09-04 まで「まだ false・法人口座待ち」と書いていた。**2 回続けて、
> 指示が実装より遅れた。** 検査への指示が古いと、検査は古い前提のまま
> 緑を返す。**レーンを回す前に、この段落が今日の事実かを確かめること。**

🔴 **未確認のまま残っている 1 点**: 契約終了（`EXPIRATION`）で `plan` が
`free` に落ちるか。**10/4 が初回。** 3 状態のうちここだけ実測が無い。

見る観点:
1. **払った人が損をする経路。**（最優先。以前の「フラグ false のとき
   導線が閉じているか」を差し替えた）
   - 購入は成立したのに `plan` が `pro` にならない
   - 解約しただけで取り上げてしまう
   - 期限が来ても `free` に落ちない（＝無料で使い続けられる）
   - 復元（Restore）が効かない・別端末で引き継げない
   🔴 **`updated_at` は webhook の順序の正本。** ここに `now()` を書く
   コードが増えていないか。書くと**より古いイベントが以後すべて落ち、
   200 を返すので再送もされない**（2026-09-04 に手動付与で踏みかけた）。
2. **掲載とフラグがずれていないか。**
   App Store の説明文・App Privacy・`PrivacyInfo.xcprivacy` とフラグは対。
   片方だけ動くと、説明文が嘘になるか審査員が導線を見つけられない。
   `purchases_gate_test` が対で見張っているが、**値は固定していない**
   （開け閉めは判断であって誤りではない）ので、**一致しているかは人が見る**。
3. 上限の書き手と読み手のキーが一致しているか。
   日次/月次の境界、タイムゾーン（JST か UTC か）、リセット時刻。
   書き込み側（DB 関数）と読み取り側（サーバのガード）で基準がずれると、
   ずれた時間帯だけ上限が効かない。
   ※ DB 関数の定義はリポジトリに無い。該当したら
     「DB 側の確認が必要」と書く。
4. 端末に上限の数字を持っていないか。
   購入画面の件数はサーバの値から取る決まり。端末に書くと env を変えた
   ときに購入画面だけが嘘をつき、それは課金の約束になる。
5. 429（上限）と 503（予算）が同じ文言にまとめられていないか。
   原因も復旧時間も違う。
6. 購入が成立したのにプランが反映されない経路が残っていないか
   （webhook の宛先・リトライ・重複）。
7. **ゲスト（未登録）は 3 か所とも閉じている**（2026-09-04）——
   アプリ `kGuestModeEnabled` / サーバ `GUEST_MODE_ENABLED` /
   **Supabase の匿名サインイン**。**アプリだけ見て「閉じている」と書かない。**
   実装は残してあるので、`if (kGuestModeEnabled)` の外に漏れた経路が
   増えていないかを見る。再開の条件は `docs/guest-mode-spec.md` §24。
8. **登録ファネル**（`/admin/analytics`・2026-09-04 追加）が、
   「読めなかった」を 0 として出していないか。0 にすると
   **離脱していないのに離脱したように見える。** 数え方は
   `funnelFrom()` に切り出してある（サーバ側 `funnel.test.ts`）。

━━━━━━━━━━ レーン 4: 写真と PII の全経路 ━━━━━━━━━━

搭乗券・パスポート・予約票が写る。流出防止がこのアプリの第一原則。

撮影 → 圧縮 → 送信 → 保存 → キャッシュ → 表示 → 共有 → 削除
の全経路を 1 本ずつ追い、各段で「残るもの」を挙げる。

見る観点:
1. 一時ファイル。image_picker / file_picker / PDF プレビューが作る中間物が
   後始末されているか。失敗経路（例外・キャンセル）でも消えるか。
2. ログ・エラー文・トースト。
   ファイル名・URL・確認番号・メールアドレス・氏名が出ていないか。
   例外をそのまま画面やログに流していないか。
3. 開発用ハーネス（lib/dev/）。
   実データが混ざっていないか。外部公開素材に使える状態か。
4. 削除の実効性。
   予定・旅程・アカウントを消したとき、Storage の実体・キャッシュ・
   端末内の残骸まで消えるか。「消したつもり」が残っていないか。
5. 送信先。
   画像・本文が渡る先が固定されているか。設定や入力で差し替わらないか。

━━━━━━━━━━ レーン 5: 境界（権限・鍵・削除）━━━━━━━━━━

見る観点:
1. RLS 前提のクエリ。
   「自分の行しか返らない」ことをサーバ側が保証しているか、
   それとも端末側の絞り込みに頼っているか。
2. 鍵・トークンの置き場。
   SharedPreferences に入っていないか。ビルドに焼き込まれていないか。
   リポジトリに平文で入っていないか。
3. 外部送信先の固定。
   ドメインが移行した箇所（curlew → junros）で、古い宛先が残っていないか。
   リダイレクト頼みになっている経路が無いか。
4. 退会・削除。
   必ず出られるか。途中で失敗したとき、黙って「成功」と表示しないか。
   消える対象の一覧と、実際に消えるものが一致しているか。
5. 認証。
   未ログインで到達できる画面・API が無いか。
   デバッグ用のバイパスが本番ビルドで死んでいるか。
6. security definer 関数（RLS を素通りする入口）。
   `revoke all from public` があるか。`grant execute` の宛先が
   service_role 等に限られているか。**authenticated に grant されている
   関数は「ログイン済みの誰でも RLS を無視して実行できる入口」。**
   引数に user_id を取り、その値を検証せずに使っていないか。
   ※ 本番にしか定義が無い関数がある。該当したら「DB 側の確認が必要」と書く。

   🔴 **`from public` だけでは閉じない。リポジトリの revoke を根拠にしない。**
   Supabase は `anon` / `authenticated` へ**ロールへ直接** EXECUTE を付ける
   既定権限を持つ。`PUBLIC` は疑似ロールなので、そこから revoke しても
   ロールの直接権限は落ちない。**必ず本番で実測する**（下の棚卸し）。

7. 管理コンソール（`https://junros.coyoteandpowell.com/admin`）。
   **全利用者のデータに届く面なので、アプリと同じ重さで見る。**
   - 誰が入れるか（`toritavi_admin_members` の行・昇格の経路）
   - 未ログイン・非管理者で到達できる画面・API が無いか
   - 画面に出る PII の範囲（写真そのものが見えないか）
   - 監査ログ（`toritavi_admin_audit_logs`）に何が残るか

━━━━━━━━ レーン 6: 検査そのものを検査する ━━━━━━━━

**検査が弱っていることは、検査からは見えない。** ここだけは対象が
アプリではなく `tool/` と `test/` になる。2026-08-16 に、この観点で
1 日に 5 件出た（プローブの対象漏れ 13 表＋1 バケット、緑で終わる抜け道 4 経路、
写しが古くなったテスト 2 件）。

見る観点:
1. **対象表・バケットの網羅。**
   `tool/rls_probe.sh` が並べる面と、**本番のスキーマに実在する面**が
   一致しているか。出どころは `supabase/*.sql` と
   `~/Dev/toritavi/supabase_migrations/*.sql` の `create table` /
   `storage.buckets`。**アプリが lib/ から触らない面ほど、
   誰も見ないまま残る**（`toritavi_admin_members` が 2026-08-16 まで対象外だった）。
2. **緑で終わる抜け道。**
   条件が揃わないときに `⚠️` や `ℹ️` を出して、全体の判定は 🟢 になる箇所。
   最終判定が `❌` だけで決まるスクリプトでは、警告はすべて素通りする。
   **「見ていない」と「安全」を同じ色で塗っていないか。**
3. **検査が空振りしていないか（最重要）。**
   「0 件だから合格」と言っている項目について、**そもそも対象データが
   存在するのか**。空の表に対する「0 件」は何も証明しない。
   前提の在庫確認が入っているか。
   ※ 2026-08-16 に新しく書いた `rls_isolation_probe.py` が、初版で
     この形だった。**直すために書いた道具が、直す対象の欠陥を持っていた。**
4. **テストの集合が、正しい源から機械的に取れているか。**
   手で写した定数（国コード・バケット名・ファイル一覧）は必ず古くなる。
   `country_support_coverage_test` / `strip_metadata_test` /
   `storage_buckets_test` がこの形で 1 度ずつ弱っている。
   **写しを見つけたら、写しを更新するのではなく、源から derive させる。**
5. **保留（⏸）の棚卸し。**
   `rls_isolation_probe.py` の `DEFERRED_REASON` は「いまのフェーズでは
   対象データが作れない」面の一覧。**フラグを開けた日に消し忘れると、
   開いた面が「作れない」の名目で素通りする。**
   `feature_flags.dart` の true/false と突き合わせる。
6. **実測が必要なものを、コード読解で済ませていないか。**
   RLS・保持期間・cron・権限は、コードが正しくても本番が違いうる。
   レーン 5 の観点 1 は**読むだけでは終わらない**——
   下の「認証済み分離プローブ」を実際に回すこと。

━━━━━━━━━━ レーン 7: 体感速度 ━━━━━━━━━━

**利用者から見て「遅い」と感じる所。** 測定値の話ではなく、
**待たされ方**の話。テストは速度を見ていないので、ここは構造的に死角。

🔴 **「遅い」には 2 種類ある。** 実際に時間がかかっているものと、
**進んでいることが見えないもの**。後者は時間が同じでも倍に感じる。
両方を所見にしてよいが、**どちらなのかを必ず書く。**

見る観点:
1. **待たせている間、進んでいることが見えるか。**
   OCR はサーバ往復で数秒かかる（`scan_screen.dart` / `ocr_service.dart`）。
   進捗・見込み・取り消しがあるか。**無言の待ちになっていないか。**
2. **UI スレッドで重い処理をしていないか。**
   画像圧縮・EXIF 除去（`core/media/strip_metadata.dart`）・
   PDF のページ画像化（`core/media/pdf_render_cache.dart` / pdfx）・base64 化。
   `compute` / isolate に出ているか。出ていないならフレームが落ちる
   ＝**スクロールが引っかかる**形で出る。
3. **同じものを何度も取っていないか。**
   `journey_repository.dart` / `journeysProvider` / `attachments.dart`。
   一覧 → 詳細で再取得していないか。**旅程ごとに添付を 1 件ずつ**引く
   形（N+1）になっていないか。
4. **キャッシュが効かない条件。**
   `journey_cache.dart` / `media_cache.dart` / `pdf_render_cache.dart`。
   **効かない側が既定になっていないか**（キャッシュはあるのに毎回外れる）。
5. **通知の貼り直しの重さ。**
   `NotificationService.reschedule()` は先頭で `cancelAll()` し、
   最大 60 件を貼り直す。`upcomingRemindersProvider` は
   **旅程か設定が変わるたびに**再計算される。
   **予定を 1 つ直すたびに 60 件貼り直していないか。**
6. **起動の直列待ち。**
   `main.dart` / `core/services/supabase_config.dart`。
   最初の画面が出るまでに、**出さなくてよいものを待っていないか。**
7. **再構築の範囲。**
   Riverpod の `watch` が広すぎて、1 文字打つたびに画面全体が作り直されて
   いないか。とくに大きい画面（`scan_review_screen.dart` /
   `journey_detail_screen.dart` / `step_ticket_screen.dart` / `tasks_tab.dart`）。
8. **失敗が確定するまでの時間。**
   タイムアウトとリトライで待ち時間が倍にならないか。
   **圏外で「ずっと回っている」**形になっていないか。

出し方（守ること）:
- 🔴 **推測の性能論を書かない。**「重そう」は所見にしない。
  **どの経路が・どの操作で・何を待たせるか**を、ファイルとシンボルで示す。
- 所見ごとに「**利用者に見える形**」を 1 行で
  （例:「読み取りを押してから数秒、画面に何も出ない」
  「一覧をスクロールすると引っかかる」）。
- 🔴 **実測していない数字を書かない。** 測っていないなら「未計測」と書く。

━━━━━━━━━━ レーン 8: 仕様の辻褄 ━━━━━━━━━━

**サービスとしての規則どうしが噛み合っているか。**

🔴 **レーン 2 との違い。** レーン 2 は「同じ事を指す 2 つの**記述**が
一致しているか」（表記の一致）。ここは「**規則が意味として噛み合うか**」。
**表記が全部一致していても、規則として辻褄が合わないことはある。**

🔴 **先に `docs/legal-data-register/CURRENT_FACTS.yaml` を読む**
（`CLAUDE.md` §4-0）。料金・プラン・上限・配信地域・公開機能はここが優先。
**矛盾を見つけても推測で直さず、CONFLICT として報告する。**

🔴 **法務文書の当否は判断しない**（`CLAUDE.md` §4-1）。
「この記載は要る／要らない」は書かない。**事実の提示で止める。**

見る観点:
1. **上限と料金。** 無料 5 件/月・Pro 50 件/月・¥780。
   上限に達した人に何が残るか。購入導線・上限表示・サーバの判定が
   同じことを言っているか。**上限の数字を端末に持っていないか**
   （`CLAUDE.md` §6 の危険物）。
2. **閉じた機能の宙ぶらりん。**
   `kTripStartNotifyEnabled` / `kGuestModeEnabled` / `kConciergeEnabled` /
   `kAffiliateEnabled` は false。**閉じた結果、行き先を失った規則・画面・
   データが残っていないか**（設定の説明文、履歴に残る記録、ヘルプの記述）。
3. **通知の一本化との整合。**
   「予定の前に通知」だけになった。通知履歴の見出し・アカウント画面の
   説明・掲載文・`docs/FEATURES.md` が同じことを言っているか。
4. **状態の出入り。**
   旅程・予定の状態に対し、**入る経路と出る経路が揃っているか。**
   片道しか無い状態が無いか（2026-08-16 の `進行中` と同型）。
5. **削除と保持。**
   90 日 purge・Storage・通知履歴・端末キャッシュ。
   「消した」と言っている所と実際に消える所が一致しているか。
   **片方だけ残る**形が無いか。
6. **配信地域と言語。**
   日本のみ配信。英語の資産・分岐が中途半端に生きていないか。
   逆に英語が要る所（法務ページは `en/` を公開済み）が欠けていないか。
7. **課金の約束。**
   Pro で何が変わるかが、購入画面・掲載文・`docs/monetization-spec.md`・
   サーバの実装で一致しているか。
8. **登録したのに何もできない経路。**
   無料は登録必須で 5 件。**上限に達した新規利用者に何が残るか。**

出し方（守ること）:
- 所見ごとに「**どの 2 つが噛み合っていないか**」を**必ず 2 点セット**で書く。
  片方だけ書かれた所見は検証できない。
- **「どちらが正しいか」は書かなくてよい。** 正本の判定は Opus 側。
  ただし**正本の宣言（「ここが正本」）がある場合は引く。**
```

### 認証済み分離プローブ（レーン 5 の実測部分）

**コードを読むだけでは、RLS が効いていることは分からない。**
匿名プローブが緑でも「ログイン済みの他人から見えるか」は何も見ていない。
2026-08-16 まで、この面は一度も測っていなかった。

🔴 **`set -a` を必ず付ける。** `~/.junros-probe.env` は `export` を含まない
素の代入なので、`source` しただけでは子プロセス（`bash tool/rls_probe.sh`）に
渡らない。**渡らないと「⚠️ 未実行」を出しつつ終了コード 0・末尾 🟢 で終わる。**

🔴 **回す直前に、A 側の種を撒く。** 撒かないと 2 件が ⛔ 検査不能で終わる ——
**空の表に対する「0 件」は何も証明しない**（レーン 6 の 3）。

```sql
-- ① 課金の表。**一度入れれば残る**（2026-09-05 に投入済み）
insert into public.toritavi_user_plan (user_id, plan, updated_at)
select id, 'free', 'epoch'::timestamptz from auth.users
where email = 'coyoteandpowell+probea@gmail.com'
on conflict (user_id) do nothing;

-- ② 分間バーストの表。🔴 **10 分で剪定されるので毎回入れ直す。**
--    入れたら**すぐ**プローブを回すこと。
insert into public.toritavi_ocr_events (user_id)
select id from auth.users where email = 'coyoteandpowell+probea@gmail.com';
```

> **①は課金を開けた（2026-09-02）のに測っていなかった面。** 09-05 に初めて
> 実測して緑になった。`plan` を **free** にしてあるのは、pro にすると検査用
> アカウントが上限 50 件になり、無料枠の挙動が測れなくなるため。
> `updated_at` は `epoch`（`now()` を書くと webhook の順序が壊れる）。

```
set -a; . ~/.junros-probe.env; set +a
bash tool/rls_probe.sh
```

> **2026-08-16 にここが間違っていた。** この節を書いた当日、
> 手順どおり（`set -a` 無し）に実行すると分離プローブが走らないまま
> 緑で終わることを実測した。`docs/subscription-setup.md` が
> `--dart-define` を案内していたのに `env.dart` は `.env` しか読まなかった
> （2026-08-02 発見）のと同じ型 —— **手順どおりでも機構に届かない。**
> 手順を書いたら、その手順で 1 度は実際に回すこと。

- テストアカウント **A（持ち主）と B（攻撃者役）** の 2 つが要る。
  資格情報は `~/.junros-probe.env`（**リポジトリの外**。中の `.env` は
  `pubspec.yaml` でアプリに同梱されるので、置くと配布物に焼き込まれる）
- 🔴 **A・B は使い捨てのアカウントに限る。** B のトークンで A の行に
  UPDATE / DELETE を試みる。RLS が正しければ 0 行だが、破れていれば
  実際に A のデータが変わる
- **⛔（検査不能）が 1 つでも残っていたら緑ではない。** A 側にデータを
  作ってから回し直す。「A が空だから 0 件」は何も証明しない
- 埋めるために A で作るもの: 旅程・予定・**スキャン画像**・連絡先（電話番号入り）・
  やることのチェック・設定の変更・フィードバック（添付付き）

### 匿名・ログイン済みの RPC 棚卸し（毎回・人が 1 本流す）

🔴 **2026-08-16 に、ここを誰も見ていなかったせいで実際に漏れていた。**
匿名キーだけで `toritavi_soft_deleted_scan_steps` が **HTTP 200 / 65 行**を返し、
`(owner_uid, step_id)` ＝ Storage のパス、つまり**全利用者のスキャン画像の所在**が
未ログインで列挙できた。削除関数 `toritavi_purge_orphan_trip_children` まで
匿名で叩けた。リポジトリの SQL には revoke が書いてあったのに、である。

`rls_probe.sh` は**読み取り専用と分かっている関数だけ**を実際に叩く。
副作用のある関数は叩けないので、**全関数の棚卸しは人が SQL Editor で行う**。

```sql
select p.proname, pg_get_function_identity_arguments(p.oid) as args,
       pg_get_function_result(p.oid) as returns,
       has_function_privilege('anon',          p.oid, 'EXECUTE') as anon_can,
       has_function_privilege('authenticated', p.oid, 'EXECUTE') as auth_can
  from pg_proc p join pg_namespace n on n.oid = p.pronamespace
 where n.nspname = 'public'
 order by anon_can desc, auth_can desc, p.proname;
```

- **`anon_can = true` が 1 つでもあれば revoke。** ただし `returns = trigger` は
  PostgREST から呼べないので対象外（`cascade_soft_delete_steps` 等）
- `auth_can = true` は「アプリが端末から呼ぶもの」だけに絞る。
  正解は `grep -rn "\.rpc(" lib/` の結果と一致すること（現在 `increment_ad_impression` の 1 本）
- 閉じ方は**両方書く**:
  ```sql
  revoke execute on function public.<名前>(<引数>) from anon, authenticated;
  ```

> ⚠️ **OpenAPI（`GET /rest/v1/` の `application/openapi+json`）で面を数える案は
> 使えない。** この本番はテーブル面も RPC 面も 0 件を返す。採用すると
> 「何を revoke しようが永久に 0 件 ✅」の空振りの緑になる（実測して不採用にした）。

### 1 レーンだけ回すとき

**上のブロックをそのまま貼って、「レーン 3 だけ」と言い足す。**

指示文の写しを別に作らないこと。「全部入り」と「レーン別」を両方置くと、
片方を直したときにもう片方が古くなる——このリポジトリが繰り返し踏んできた形
そのものになる。

### 受け取ったあと（Opus 側）

1. **1 件ずつ真偽判定。** 「これが偽である条件」から先に確認する
2. 真のものだけ着手。**偽が半分を超えたレーンは、直すのではなく
   そのレーンの指示を書き直す**
3. 直す順序は必ず「**出ることを固定するテストを先に書く**」。
   「落ちない」テストは出ないことも通してしまう

### 記録

回した週・所見件数・そのうち真だった件数を残す。
**真の率が下がってきたレーンは枯れたと判断して、次回は飛ばす。**

| 週 | レーン | 所見 | 真 | 備考 |
|---|---|---|---|---|
| 2026-08-02 | 1〜5（初回） | 15 | **15** | 致命 0 / 高 2 / 中 8 / 低 5。**偽は 0。全 15 件を同日に解決。** 収穫は 1-2（オフライン起動で通知が全消え）／1-1（削除した行が永続。しかも素直に直すと写真が孤立する）／3-2（鍵の注入手順そのものが効いていなかった） |
| 2026-08-16 | 1〜5 ＋ 提出前 | 67 | **67** | 致命 1 / 高 12 / 中・低 54。**偽は 0。** 致命はフィードバック添付の **EXIF/GPS 素通り**（同じ欠陥を 5 日前に別経路で直したばかり）。**しかもそれを隠していたのは自分で書いたテスト**——`strip_metadata_test` が 1 ファイルだけを固定していた |
| 2026-08-16 | 分離（レーン 5 実測） | 6 | **6** | Fable による RLS 分離の専用検査。**破れは 0**（13 表・3 バケット・権限昇格を実測）。収穫は検査側で、プローブが **13 表中 1 表の read しか見ておらず**、条件が揃わないと緑で終わる抜け道が 4 経路あった。**レーン 6 を新設** |
| 2026-08-30 | 4（写真と PII・単独） | 9 | **9** | **偽は 0。** 致命 0 / 中 4 / 低 5。同日未明のコミット `2c60e3d` で 8 件を直した**あと**に残っていたもの。収穫は **正本が「3 つとも同時に直した」と書いた注記のうち 1 つ（`attachments.dart`「GPS ピンを持たず」）が実際には直っていなかった**こと —— **直した記録の方が嘘をついていた**形。次に pdfx が搭乗券のページ PNG を `Documents/pdf_renderer_cache/` へ書き、**掃く箇所がアプリに 1 つも無い**（陽性対照つきで確認）。`debugPrint` 4 箇所が**リテラルの `\$e`** で原因が一切残らない |
| 2026-09-13 | **7・8（新設・初回）** | 31 | **4 / 31 判定済み** | レーン 7 は **4 周目でも新規 8 件＝枯れていない**（3 周目 4 件 → 4 周目 8 件で**増えた**）。 レーン 8 は 3 周目ゼロで枯れた。**判定した 4 件はすべて真**で、同日に直した。 収穫は **7-1**（09-09 に一覧だけ直した締切が、中核である旅程の中身に入っていなかった）と **8-1**（サーバは PDF を 5 ページ＝1 件で数えるのに、送る前に件数を知る手段がアプリに 1 つも無かった。正本が指す `/api/ocr/preflight` は**作られていない**）。内訳は下記 |

> **1-4 は「保留」から「真」へ上げた。** 当初は「今は無害・将来の形」と
> 判定したが、無害なのは*たまたま*で、欠陥（同じ表の 4 重定義・うち 1 つが
> 相違）は実在した。**次にカテゴリを足した日に効かなくなる**のは予測ではなく
> 構造の帰結。真偽判定では「今日壊れているか」ではなく
> 「所見が指す構造が実在するか」で見る。

### 2026-09-13 の内訳（レーン 7・8）

🔴 **所見の本文はここに要約だけ置く。** 全文は Fable の返答にしかなく、
セッションが切れると復元できない。**次に回す人が判断できる粒度**で残す。

**直したもの（すべて真・同日）**

| # | 何が起きていた | 直し方 | コミット |
|---|---|---|---|
| 8-8 | 台帳の現在値が 2 世代前（版・第三者ログイン）。**しかも突き合わせ検査の側が片方向**で、公開後は正しい台帳を落とす向きに働いていた | 台帳を現在値へ。検査を両方向の数値比較に | `ec47f1e` |
| 7-1 | 締切があるのは一覧（`getJourneys`）だけ。詳細・スキャン元・添付一覧・添付の中身は**控えへ落ちる `catch` を持っているのに失敗しない** —— 応答しない Wi-Fi で回り続ける。機内で便名が見られない | 5 経路に締切（行 10 秒 / ファイル 60 秒）。控えを持つ読み取りを表で数え切る検査 | `61c8f65` |
| 8-1 | サーバは PDF を **5 ページごとに 1 件**消費するのに、アプリは「読み取り 1 件」としか言わない。残り 5 件で 11 ページの PDF を送ると残りが 2 になる。**正本が指す `/api/ocr/preflight` は存在しない** | 選んだ時点で「N ページ・読み取り M 件」。サーバの `ocr-limits.ts` を実際に読んで突き合わせる検査（写しを置いてよい条件） | `fbfafef` |
| 7-12 | アカウント画面が、残数が返るまで**アップグレードの行ごと描かない**。読み込み中・失敗・オフラインが全部「何も無い」。**上限前の無料会員が Pro へ行ける導線はここだけ** | 3 状態に分ける。隠すのは Pro と分かったときだけ | `3fe18e4` |

> **8-1 の直しには利用者の判断が要った。** サーバの正本が
> 「アプリ側にこの数字を写すな」と**名指しで禁止**しており、理由も正しい。
> 写すと決めたのは利用者で、**代わりに突き合わせ検査を置く**ことが条件。
> サーバ側の注記も書き換えた（`~/Dev/toritavi/app` の `daa719c`・**未 push**）。

**未判定（真偽判定から）**

| # | 深刻度 | 一言 |
|---|---|---|
| 7-9 | 中 | 旅程詳細を開くと、独立した 3 本の GET を 1 本ずつ順番に待つ（`tripSuggestionsProvider`） |
| 7-3 | 中 | 予定 1 件の変更が 4〜5 往復の直列になり、その間に画面の手がかりが無い |
| 7-4 | 中 | 旅程を開くと本体を 2〜3 回取り、各回で全予定のスキャン元参照を N 回引き直す |
| 7-5 | 中 | 起動が `runApp()` の前で RevenueCat への通信を待っている |
| 7-2 | 中 | PostgREST の GET は圏外でも 4 回試し、待機だけで 7 秒経ってから例外になる |
| 7-6 | 低 | 一覧が invalidate されるたびに `cancelAll()` ＋最大 60 件の貼り直し |
| 7-7 | 低 | スキャン元画像は控えがあってもオンラインでは毎回外れる |
| 7-8 | 低 | `compute`/Isolate が 0 件。base64・jsonEncode・TTF 解析・PDF 生成が UI isolate |
| 7-10 | 低 | やることを 1 件チェックするたび、旅程本体＋予定数ぶんの通信が走る |
| 7-11 | 低 | 一覧・詳細が、控えファイルの書き戻しを**待ってから**画面に返す（写真の控えは待っていないのに） |
| 8-2 | 中 | アプリの残数表示が Pro にも「今月」と言う（サーバは Pro に「今月」と言わない） |
| 8-3 | 中 | 503（月予算）の経路で「Pro は予算で止めない」「行き止まりにしない」が画面に届いていない |
| 8-4 | 中 | 通知の「現在仕様」を 2 つの一次情報が別々に書いている |
| 8-5 | 中 | 「放置口座こそ確実に消す」保持期限と、TTL の無い端末の写真控え |
| 8-6 | 中 | 「退会で全部消える・必ず出られる」と「解約は Apple 側でしか行えない」 |
| 8-7 | 中 | 「写真もオフラインで見られる」を書かない正本と、書いている掲載用正本 |
| 8-9 | 低 | 「登録しない場合も月 5 件」の「登録」が、アプリの「登録」と衝突する |
| 8-10 | 低 | RENEWAL のたびに `period_anchor` を書き直す webhook と、月末を丸める DB 関数（推測を含む） |
| 8-11 | 低 | 英語 UI と、日本語でしか返らない上限文言（配信は日本のみ・既知） |

🔴 **8-8 は CONFLICT として上がってきたもの**（Fable が「どれが正しいかは
判定しない」と書いて渡した）。`UNKNOWNS.md` の `CONF-002` は 1.3.0 以後の
状態を持っていないので、**そちらの更新はまだ**。

**レーン 7・4 周目で足された 8 件（すべて未判定）**

3 周目が自分で「見ていない」「示せない」と書いた場所を名指しで当てて出た。
**このやり方は 2 周続けて効いている。**

| # | 深刻度 | 一言 |
|---|---|---|
| 7-13 | 中 | 🔴 **残数（`/api/ai-usage`）に締切が無い。** 生きている経路で締切が無い `http` はここ 1 本だけ。**09-13 に骨組みを出すようにした（`3fe18e4`）ので、止まると今度は骨組みが永久に見える** —— 読み込み中には取り直す口が無い |
| 7-14 | 中 | 書類なしの「作成」が、**読み取り用の待機画面**を出す。「N 秒経過・目安 20〜40 秒」の N は前回の読み取りのまま凍結し、「キャンセル」は作成を止めないのに「読み取り回数を消費することがあります」と言う |
| 7-15 | 中 | 確認画面の「登録する」が、予定数 ×（6〜7 往復＋ページ数ぶんのアップロード）を直列に待つ。**同じ PDF のページを予定ごとに上げ直す**（10 ページ×3 予定で 30 回超） |
| 7-17 | 中 | 一覧の検索 1 打鍵で `journeyState` が数百回走る。**実測あり**（Mac・JIT）: 旅程 30 件で 684 回・**中央値 93 ms**（1 フレームは 16.7 ms） |
| 7-16 | 低 | 写真 1 枚を出すたび、控えの書き込みと控えディレクトリの全走査を**表示の前に**待つ |
| 7-18 | 低 | 写真・ファイルを選んだあと、一覧に積まれるまで画面が変わらない |
| 7-19 | 低 | 添付を開くとき、押した行には何も出ず、スピナーは離れた「ファイルを追加」に出る |
| 7-20 | 低 | インラインの書類プレビューが元の寸法で復号される（`cacheWidth` 無し）。同じリポジトリの別の 2 か所は付けていて理由も書いてある |

> **偽と確認して所見にしなかったもの**（3 周目の「示せない」の片付け）——
> `initializeDateFormatting()`（全ロケール）は **9 ms**、タイムゾーン表は
> **17 ms**（Mac 実測）。背景の `recoverSession()` が終わる前の API 呼び出しも
> 所見にならない（`AuthHttpClient` が毎回期限を見て更新を待ち、
> gotrue が同じ要求を 1 本に束ねる）。
>
> **レーン外の付記 2 件**（体感速度ではない・真偽判定は別レーンで）——
> ① `MediaCache` の目録が read-modify-write で並行し、**後勝ちで項目が落ちうる**
> ② 一括読み取りの PDF は 1 ページ目を 2 回上げており、
> **チケット画面に 1 ページ目が 2 回出るはず**（実機未確認）

**🔴 測り方が決まった**（4 周目の収穫。申し送りの宿題への答え）

`tool/bench/` に置いた。**`test/` には置かない** —— 数字は端末とビルド種別で
変わるので、閾値を固定すると「環境が違うだけで赤い」検査になって消される。
`flutter test` は `test/` しか見ないので全件の時間も増えない。

| ファイル | 測るもの |
|---|---|
| `tool/bench/journey_list_bench_test.dart` | 一覧の 1 打鍵ぶんの計算（`journeyState` の回数と時間） |
| `tool/bench/startup_bench.dart` | 起動時の端末内初期化 |

🔴 **どちらも Mac の数字。実機はまだ 1 度も測っていない。**

**レーン 7 に残っている宿題**

| 項目 | 状態 |
|---|---|
| 5 周目（「枯れた」判定） | **未了。** 4 周目でも新規 8 件が出ており、**まだ枯れていない**（3 周目 4 件 → 4 周目 8 件で**増えている**） |
| 実機での計測 | **未了。** Mac では 2 つ測れた（`tool/bench/`）が、実機は 0 件 |
| DB・ダッシュボード側の確認（人） | **未了。** `entry_requirements` の行数と列の適用状態／JWT の有効期限の本番値 |

### 2026-08-02 の内訳

**直したもの（コード 3 件・いずれもテスト先行）**

| # | 何が起きていた | 直し方 |
|---|---|---|
| 1-2 | 機内モードで起動すると `journeysProvider` のエラーが `?? []` で「予定 0 件」に化け、`reschedule([])` → `cancelAll()` で**貼ってあった通知が全部消えた**（復帰しない） | `upcomingRemindersProvider` を `List<Reminder>?` にして「まだ分からない」と空を区別。`reminder_wipe_guard_test.dart` |
| 1-3 | 「元に戻す」が失敗しても何も出ず、戻らない（4 箇所すべて `onUndo` に try 無し） | `showUndoToast` 側で捕まえる（`_runUndo`）。全員が通る 1 箇所で直す。`toast_test.dart` に 2 件追加 |
| 4-1 | `XFile.fromData` を渡すと share_plus が tmp に**実体を書き出し、誰も消さない**。搭乗券・パスポートが端末に残る | 自分で一時ディレクトリを作り `finally` で消す。`share_temp_cleanup_test.dart` |

**直したもの（文書 7 件）**: 2-1 フェーズ表の内部矛盾 / 2-2 直した欠陥の残存 2 か所 /
2-3 アフィリの列名（`deeplink_template`）/ 2-4 よく使う場所 / 2-5 wip ブランチ /
3-3 503 の文言（サーバ）/ 4-2 署名 URL / 5-1 debug_mode のコメント。

**未決（人の判断・DB 確認が要る）**

- ~~**1-1**~~ **同日に解決**（`023_purge_soft_deleted_safely.sql` 適用済）。
  cron を実測 → 未登録を確認 → 関数定義を読んだところ **Storage に触らない**
  ことが分かり、**そのまま cron を足すと写真が孤立する**と判明した。
  順序の約束ではなく関数の前提条件で塞いだ。詳細は下の「1-1 の教訓」
- ~~**2-3 の危険部分**~~ **成立せず**。実列は 8 つで `url_template` は存在
  しない（誤った列名で `update` を書けばその場でエラー＝フェイルラウド）
- ~~**3-1**~~ **同日に解決**。ストアとサーバのプランのずれを
  `subscription_sync.dart` で判定し、10 分（`kPlanSyncGrace`）を過ぎたら
  アカウント画面に「ご契約がまだ反映されていません」を出す。
  **`unknown`（サーバに聞けていない）を `stalled` に倒さない**のが要点で、
  圏外で嘘の警告を出さない。シミュレータで両状態を確認済み
- ~~**3-2**~~ **同日に解決。しかも所見より悪かった。**
  「鍵の未注入が無言で準備中になる」だけでなく、**注入する手順そのものが
  効かなかった** —— `subscription-setup.md` は
  `--dart-define=TORITAVI_REVENUECAT_IOS_API_KEY` と書いていたのに、
  `env.dart` は `.env` の `REVENUECAT_IOS_API_KEY` しか読んでいなかった
  （供給経路も名前も違う）。手順どおりビルドしても鍵は入らない。
  dart-define を優先し `.env` へフォールバックする形にし、
  `revenuecat_key_contract_test.dart` が**手順書と `env.dart` を突き合わせる**。
  鍵が空のまま `kSubscriptionEnabled=true` なら debug で assert が止め、
  release でもログに残す
- ~~**1-4**~~ **同日に解決**。移動系カテゴリを `kMovingCategories`
  （models.dart）に一本化。`reminder_builder` だけ集合が違ったが**挙動は
  変えず**、差（徒歩を除く／旧データのタクシーを足す）を派生として書いた。
  `moving_categories_test.dart` が写しの再発を止める

**残件はゼロ。** 課金を開ける日にやることは
`docs/feature-flags.md` §1.2 の 6 条件に全部書いてある。

### 1-1 の教訓（次の検査でも使う）

**「動いていない安全装置を動かす」ときは、動かした先で何が壊れるかを先に見る。**

所見は「cron が未設定」で止まっていた。そこで cron を足していたら、
**テキストの残留を写真の永久残留と交換していた**（retention は step 行を辿って
写真を列挙するので、行が先に消えると二度と見つからない）。
関数定義を読むまで分からず、定義はリポジトリに無かった。

指示文の禁止事項 4「リポジトリに定義が無い本番オブジェクトについて、
コードだけを見て断定しない」が効いた形。**検査側が正しく「DB 側の確認が必要」
と書き、実装側がそれを実際に確認した**ので、事故にならずに済んだ。

**次回への申し送り**: レーン 1 は 2 周連続ゼロで枯れた。レーン 2〜5 は
1〜2 周で切り上げているので、次回はレーン 2 から始めて枯らす。

**2026-09-13 の申し送り**: **レーン 7（体感速度）と 8（仕様の辻褄）を新設**
（利用者の指示）。回し終わったので、結果を「記録」に入れた。

🔴 **次回はレーン 7 の 5 周目から。** 4 周目でも新規 8 件が出ており、
**枯れていない。しかも増えている**（3 周目 4 件 → 4 周目 8 件）。

🔴 **当て方が分かった。2 周続けて効いている。** 観点 1〜8 をもう一度
なぞるのではなく、**前の周が自分で「見ていない」「示せない」と書いた場所を
名指しで当てる。** 4 周目はそれに加えて、**その周に直したばかりのコード**を
見直して 1 件出した（7-13）。次も同じやり方で
（`lib/dev` と閉じた機能は確認済みなので飛ばしてよい）。

✅ **測り方は決まった**（`tool/bench/`。4 周目の収穫）。**ただし Mac だけ。**
実機での計測はまだ 1 度も無い。数字を書くときは**どこで測ったか**を必ず添える。

🔴 **「直した直後のコード」を次の周で見ること。** 7-13 は、09-13 に
アカウント画面へ骨組みを出すようにした（`3fe18e4`）**結果として**、
締切の無い残数取得が「永久に回る骨組み」として見えるようになったもの。
**直しが新しい症状を作る**形は、直した本人には見えない。

**偽は出なかった。** 判定した 4 件はすべて真。ただし **4 / 23 しか
判定していない**ので、「レーンの質」の結論はまだ出せない。
レーン 8 の所見 8 は Fable が**自分で判定せず CONFLICT として渡した** ——
指示文の禁止事項がそのまま働いた形で、この形は残す。
